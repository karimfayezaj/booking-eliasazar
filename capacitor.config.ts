import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zorua.bookies',
  appName: 'Bookies',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
